node_modules/
.DS_Store
*.log
tests/private/
docs/energy-viz.html
docs/rankfusion_with_awk.md
explorer/node_modules/
explorer/dist/
explorer/public/demo/
demo/truth/
/dist/release/

# — local Claude Code / tool state (never shipped) —
.claude/
.entire/

# Cartographer's own event logs. These are agent-session transcripts, which auto
# mode's classifier treats as sensitive data that belongs in no repo — public or
# private, this one included. A commit here trips the classifier, and rightly so.
#
# If a project ever wants its chat history versioned deliberately, un-ignore that
# project's own path rather than removing this rule — the default for a repo that
# happens to be *running* cartographer must stay "don't commit the logs."
.carto/

# claude-mem auto-generated per-dir context stubs (plugin is disabled; these are leftovers).
# Keep the real root CLAUDE.md tracked.
CLAUDE.md
!/CLAUDE.md

# excalidraw skill's vendored fonts (24 MB Excalifont/Virgil) — dropped here by the skill, not part of SC.
scripts/excalidraw/
.agents/skills/excalidraw/
